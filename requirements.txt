# requirements.txt for Employee Attrition Analysis App

# Core data analysis and visualization libraries
pandas>=1.0.0
numpy>=1.18.0
matplotlib>=3.0.0
seaborn>=0.10.0

# Excel file handling
openpyxl>=3.0.0  # For reading Excel files with pandas

# Streamlit for deployment
streamlit>=1.0.0

# Machine learning libraries (though not used in current code, often needed for similar analyses)
scikit-learn>=0.22.0

# Jupyter notebook dependencies (not strictly needed for deployment but good to have)
ipykernel>=5.0.0
jupyter>=1.0.0
